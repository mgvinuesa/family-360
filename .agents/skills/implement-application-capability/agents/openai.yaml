interface:
  display_name: "Implement Application Capability"
  short_description: "Implement scoped use cases and domain behavior"
  default_prompt: "Use $implement-application-capability to implement this already scoped application change with one explicit UseCase class per business action."
